#include "main.h"
#include <stdio.h>
/**
 * _strlen - length of string
 * @s: character variable
 * Return: length
 */
int _strlen(char *s)
{
int i;
for (i = 0 ; i != '\0' ; i++)
{
continue;
}
return (i)
}
