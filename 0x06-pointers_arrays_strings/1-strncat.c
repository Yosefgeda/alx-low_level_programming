#include "main.h"
#include <stdio.h>
/**
 * _strncat - check the code
 * @dest: char variable
 * @src: char variable
 * @n: int variable
 * Return: value
 */

char *_strncat(char *dest, char *src, int n)
{
int i, j;

for (dest[i] = 0 ; i != '\0' ; i++)
{
printf("%s", dest[i]);
}
n < j;
for (src[j] = 0 ; j != '\0' ; j++)
{
printf("%s", src[n]);
}
return (dest);
}
