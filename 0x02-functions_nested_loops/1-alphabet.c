#include <stdlib.h>
#include <stdio.h>

/**
 * main - print alphabet
 * Return: value from a to z
 */

int main(void)
{
        void print_alphabet(void);
	{
		char ch;
		for (ch = 'a' ; ch<= 'z' ; ch++)
			putchar(ch);
			return(ch);
	}
        return (0);




}

