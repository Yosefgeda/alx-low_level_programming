#include <stdlib.h>
#include <stdio.h>

/**
 * main - print alphabet
 * Return: value from a to z
 */
	int print_alphabet(void)
	{
		char ch = 'a';
		while (ch <= 'z')
		{
			putchar(ch);
			ch++;
		}		
		return;
	}
	int main()
{

{
   void print_alphabet(void);
	putchar('\n');
	return (0);
}
}
