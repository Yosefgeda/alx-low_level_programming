#include <stdlib.h>
#include <stdio.h>
#include "main.h"
#include "main.c"
#include <unistd.h>
/**
 * main - print alphabet
 * Return: value from a to z
 */
int main(void)
{
	void print_alphabet(void);
	putchar('\n'):
	return (0);
}
        void print_alphabet(void)
	{
		char ch = 'a';
		while (ch <= 'z')
		{
			putchar(ch);
			ch++;
		}	
		
	}

