#include <stdlib.h>
#include <stdio.h>

/**
 * main - to excute printing of single digit numbers
 * Return: prints all single digit numbers starting from 0
 */
int main (void)
{
	int n = 0;
	
	for (n = 0 ; n < 10 ; n++)
		printf("%d", n);
	printf("\n");
	return (0);



}
