#include <stdio.h>
#include <stdlib.h>
/**
 * main - function
 * @argc: argument to main
 * @argv: argumrnt to main
 * Return: 0
 */
int main(int argc, char *argv[])
{
int i;
int sum = 0;

for (i = 0 ; i < argc ; i++)
{
	if (i = 0)
		printf("0\n");
	else if (!isdigit(i))
		printf("Error\n");
		return (1);
	else if 
	{
		sum = sum + atoi(argv[i]);
	}
		printf("%d\n", sum);
}
}
